{
  "scripts": {
    "test": "npx cypress run --reporter mochawesome",
    "clean-reports": "rimraf \"mochawesome-report\"",
    "test:clean": "npm run clean-reports && npm run test",
    "open-reports": "start mochawesome-report\\mochawesome.html",
    "test:report": "npm run clean-reports && npm run test && npm run open-reports"
  },
  "devDependencies": {
    "@types/cypress": "^1.1.6",
    "@types/node": "^24.3.1",
    "cypress": "^15.1.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-xpath": "^2.0.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.2",
    "open": "^8.4.0"
  }
}
